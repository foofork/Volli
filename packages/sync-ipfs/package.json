{
  "name": "@volli/sync-ipfs",
  "version": "0.1.0",
  "description": "IPFS-based synchronization for Volli",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@volli/vault-core": "workspace:*",
    "@volli/identity-core": "workspace:*",
    "ipfs-core": "^0.18.0",
    "ipfs-core-types": "^0.14.0",
    "ipfs-http-client": "^60.0.0",
    "it-all": "^3.0.0",
    "multiformats": "^12.1.0",
    "uint8arrays": "^4.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "vitest": "^1.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}