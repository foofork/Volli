{
  "name": "@volli/cap-table",
  "version": "0.1.0",
  "description": "Cap table and equity management for Volli",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@volli/identity-core": "^0.1.0",
    "@volli/vault-core": "^0.1.0",
    "date-fns": "^2.29.3",
    "decimal.js": "^10.4.3",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "vitest": "^1.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}